
<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.HabitZen" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary colors -->
        <item name="colorPrimary">@color/primary_60</item>
        <item name="colorPrimaryVariant">@color/primaryDark_60</item>
        <item name="colorOnPrimary">@color/on_primary</item>

        <!-- Secondary colors -->
        <item name="colorSecondary">@color/secondary_30</item>
        <item name="colorSecondaryVariant">@color/secondaryDark_30</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Surface colors -->
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnSurface">@color/on_surface</item>
        <item name="colorSurfaceVariant">@color/surface_variant</item>
        <item name="colorOnSurfaceVariant">@color/on_surface_variant</item>

        <!-- Background -->
        <item name="android:colorBackground">@color/background_primary</item>
        <item name="colorOnBackground">@color/on_surface</item>

        <!-- Accent -->
        <item name="colorAccent">@color/new_primary</item>

        <!-- Error -->
        <item name="colorError">@color/error</item>
        <item name="colorOnError">@color/white</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">#8074F2</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Navigation bar -->
        <item name="android:navigationBarColor">#8074F2</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>

    <style name="Theme.HabitZen.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.HabitZen.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.HabitZen.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Custom Card Style -->
    <style name="HabitZen.Card" parent="Widget.Material3.CardView.Elevated">
        <item name="cardCornerRadius">16dp</item>
        <item name="cardElevation">4dp</item>
        <item name="cardUseCompatPadding">true</item>
        <item name="cardBackgroundColor">@color/card_background</item>
    </style>

    <!-- Modern Button Styles -->
    <style name="HabitZen.Button.Primary" parent="Widget.Material3.Button">
        <item name="backgroundTint">@color/primary_60</item>
        <item name="android:textColor">@color/on_primary</item>
        <item name="cornerRadius">12dp</item>
    </style>

    <style name="HabitZen.Button.Secondary" parent="Widget.Material3.Button.OutlinedButton">
        <item name="strokeColor">@color/primary_60</item>
        <item name="android:textColor">@color/primary_60</item>
        <item name="cornerRadius">12dp</item>
    </style>
</resources>
